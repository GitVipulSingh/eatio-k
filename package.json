{
  "name": "eatio-fullstack",
  "version": "1.0.0",
  "description": "Full-stack food delivery application with React frontend and Express backend",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm install --prefix eatio-backend && npm install --prefix frontend",
    "dev": "concurrently --kill-others-on-fail \"npm run dev --prefix eatio-backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "concurrently --kill-others-on-fail \"npm run start --prefix eatio-backend\" \"npm run start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix eatio-backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "setup": "npm run install-all && echo ✅ Setup complete! Run 'npm run dev' to start development"
  },
  "keywords": [
    "food-delivery",
    "react",
    "express",
    "mongodb",
    "fullstack"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}